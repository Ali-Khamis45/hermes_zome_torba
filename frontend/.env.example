# Copy to .env.local for `npm run dev` outside Docker Compose. When run via docker-compose.yml at
# the repo root, these are injected as container environment variables instead — see
# infrastructure/docker/frontend.Dockerfile.

NEXT_PUBLIC_API_URL=http://localhost:5080
NEXT_PUBLIC_SIGNALR_URL=http://localhost:5080/hubs
