# git-agent

Clone, commit, push, pull, branch, merge, open pull requests, and assist with conflict resolution
— the agent the Coder/Reviewer multi-agent roles (see [docs/18-roadmap.md](../../docs/18-roadmap.md)
Phase 3) drive for source control operations.

**Status:** Phase 2/3 implementation target. `taskType` values: `git.clone`, `git.commit`,
`git.push`, `git.pull`, `git.branch`, `git.merge`, `git.openPullRequest`.
