# docker-agent

Containers, images, volumes, networks, compose stacks, logs, exec, and build — used by workflows
and the Docker dashboard integration listed in the platform brief.

**Status:** Phase 2/3 implementation target — see [docs/18-roadmap.md](../../docs/18-roadmap.md).
`taskType` values: `docker.containers.list`, `docker.containers.exec`, `docker.images.build`,
`docker.compose.up`, `docker.logs.stream`.
